import { Box, Typography } from "@mui/material";



const MovieDetails = ({ movieData }: { movieData: any }) => {
    return (
        <Box>
            <Typography> DETAILS </Typography>
        </Box>
    )
};

export default MovieDetails;